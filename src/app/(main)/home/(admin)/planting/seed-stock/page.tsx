"use client";
import { useRouter } from "next/navigation";

export default function SeedStokPage() {
    const router = useRouter();
    return (
        <div>
            Stok Bibit
        </div>
    );
}